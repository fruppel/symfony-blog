{% extends 'base.html.twig' %}

{% block content %}
    <h1 class="text-lg mb-4">Blogeinträge</h1>
    {% for post in posts %}
        <div class="mb-3">
            <h2>{{ post.title }}</h2>
            <p>{{ post.body }}</p>
        </div>
    {% endfor %}
{% endblock %}
